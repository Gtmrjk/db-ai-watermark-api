{
  "name": "db-ai-watermark-api",
  "version": "1.0.0",
  "description": "HTTP API wrapper around the DB A.I. Watermark Compositor browser UI.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "explore": "node scripts/explore-flow.js",
    "check": "node --check src/server.js && node --check src/watermark-agent.js && node --check scripts/explore-flow.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "4.19.2",
    "multer": "1.4.5-lts.1",
    "playwright": "1.44.1"
  }
}
